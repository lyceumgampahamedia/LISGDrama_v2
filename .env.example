# No PayHere frontend environment value is required. This verification build
# is locked to https://www.payhere.lk/pay/checkout in src/config.js.
# Never place a Merchant Secret, SMTP password, service account or App Check
# debug token in a Vite environment file.
