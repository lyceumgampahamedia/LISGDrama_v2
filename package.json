{
  "name": "katanayaka-booking-system",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/seat-config.test.js tests/seat-availability.test.js",
    "build:functions": "npm --prefix functions run build",
    "verify": "npm test && npm run build && npm --prefix functions run test:payhere"
  },
  "dependencies": {
    "firebase": "12.17.1",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.0.5",
    "vite": "8.2.1"
  }
}
